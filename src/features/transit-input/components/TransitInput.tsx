import { FC, ReactElement } from 'react';

const TransitInput: FC = (): ReactElement => {
  return (
    <div className="bg-green-300 w-full" style={{ height: '500px' }}>
      TransitInput
    </div>
  );
};

export default TransitInput;
