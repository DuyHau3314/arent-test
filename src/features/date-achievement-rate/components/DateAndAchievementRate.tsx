import { FC, ReactElement } from 'react';

const DateAndAchievementRate: FC = (): ReactElement => {
  return (
    <div className="bg-orange-400 w-full" style={{ height: '500px' }}>
      DateAndAchievementRate
    </div>
  );
};

export default DateAndAchievementRate;
